---
import LogoDark from "@assets/logo-dark.svg";
import LogoLight from "@assets/logo-light.svg";
import MenuIcon from "@assets/icons/menu.svg";
import CloseIcon from "@assets/icons/close.svg";
import MeetIcon from "@assets/icons/google-meet.svg";
import Button from "@components/Button.astro";
import { primaryNav as links } from "@constants/nav.ts";
---

<header class="fixed inset-x-0 top-0 z-40">
	<input type="checkbox" id="menu-toggle" class="peer sr-only" />
	<div
		class="relative z-50 border-b border-blue-50 bg-grey-2 px-8 py-8 transition-colors duration-300 peer-checked:border-blue-400 peer-checked:bg-blue-500 motion-reduce:transition-none md:px-6 xl:px-0"
	>
		<div class="mx-auto flex w-full max-w-7xl items-center">
			<a
				href="#top"
				aria-label="Fayda Studio home"
				class="relative flex flex-1"
			>
				<LogoDark class="menu-logo-dark h-8 w-auto" />
				<LogoLight
					class="menu-logo-light absolute left-0 top-0 h-8 w-auto opacity-0"
				/>
			</a>
			<nav aria-label="Primary" class="hidden items-center gap-8 lg:flex">
				{
					links.map((link) => (
						<a
							href={link.href}
							class="text-medium-sm font-medium text-grey-10"
						>
							{link.label}
						</a>
					))
				}
			</nav>
			<div class="hidden flex-1 justify-end lg:flex">
				<Button label="Book a 15-Min Call">
					<MeetIcon slot="icon" class="h-3 w-[14px]" />
				</Button>
			</div>
			<label
				for="menu-toggle"
				class="flex cursor-pointer items-center gap-2 lg:hidden"
				aria-label="Toggle menu"
			>
				<span class="relative block">
					<span
						class="menu-label-open block text-body-sm text-grey-10"
						>Menu</span
					>
					<span
						class="menu-label-close absolute inset-0 block text-body-sm text-grey-1 opacity-0"
						>Close</span
					>
				</span>
				<span class="relative block h-4 w-4">
					<MenuIcon class="menu-icon-open absolute inset-0 h-4 w-4" />
					<CloseIcon
						class="menu-icon-close absolute inset-0 h-4 w-4 opacity-0"
					/>
				</span>
			</label>
		</div>
	</div>
	<div
		id="mobile-menu"
		class="fixed inset-0 z-40 flex -translate-y-full flex-col bg-blue-500 px-8 pb-8 pt-28 transition-transform duration-500 ease-in-out peer-checked:translate-y-0 motion-reduce:transition-none lg:hidden"
	>
		<nav aria-label="Mobile" class="mt-8 flex flex-col justify-center">
			{
				links.map((link) => (
					<label for="menu-toggle" class="cursor-pointer">
						<a
							href={link.href}
							class="block border-b border-blue-400 py-8 text-center font-display text-mobile-h2 font-semibold text-grey-1"
						>
							{link.label}
						</a>
					</label>
				))
			}

			<div class="mb-8 mt-auto pt-8 px-8">
				<label for="menu-toggle" class="block cursor-pointer">
					<Button label="Book a 15 min Call" className="w-full">
						<MeetIcon slot="icon" class="h-3 w-[14px]" />
					</Button>
				</label>
			</div>
		</nav>
	</div>
</header>
